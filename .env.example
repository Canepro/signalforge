# --- LLM provider (default: openai) ---
# LLM_PROVIDER=openai

# OpenAI (direct) — when LLM_PROVIDER is unset or openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# Azure OpenAI Responses — when LLM_PROVIDER=azure
# Deployment name is sent as the Responses `model` field.
#
# Pattern A — legacy resource root (Cognitive Services or openai.azure.com without /openai/v1):
#   AZURE_OPENAI_API_VERSION is required (e.g. 2025-04-01-preview).
# AZURE_OPENAI_ENDPOINT=https://your-resource.cognitiveservices.azure.com
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_API_VERSION=2025-04-01-preview
# AZURE_OPENAI_DEPLOYMENT=gpt-5.4-mini
#
# Pattern B — OpenAI v1 base URL (no extra /openai suffix; do NOT set API version — Azure rejects api-version on v1):
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/openai/v1/
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_DEPLOYMENT=gpt-5.4-mini
#
# Pattern C - Azure AI Foundry project endpoint (SignalForge appends /openai/v1; do NOT set API version):
# AZURE_OPENAI_ENDPOINT=https://your-resource.services.ai.azure.com/api/projects/your-project
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_DEPLOYMENT=gpt-5.4

# --- API persistence ---
# Default local backend:
# DATABASE_DRIVER=sqlite
# DATABASE_PATH=./signalforge.db
#
# Postgres backend:
# DATABASE_DRIVER=postgres
# DATABASE_URL=postgres://user:password@host:5432/signalforge
#
# Container / ACA runtime bind:
# PORT=3000
# HOSTNAME=0.0.0.0
#
# Optional artifact upload guard. Defaults to 50 MiB.
# SIGNALFORGE_MAX_ARTIFACT_BYTES=52428800
#
# Optional direct runs API auth for hosted/public deployments.
# When enabled, direct /api/runs submit/list/read/report/compare routes accept
# Authorization: Bearer <SIGNALFORGE_RUNS_API_TOKEN>, admin Bearer, or the admin session cookie.
# SIGNALFORGE_RUNS_REQUIRE_AUTH=false
# SIGNALFORGE_RUNS_API_TOKEN=
#
# Optional hosted posture: keep only / public, require auth for operational pages
# and machine routes. ACA defaults this on through the deploy template.
# SIGNALFORGE_PUBLIC_LANDING_ONLY=false

# --- Phase 6c: operator API + Sources UI (required for /api/sources, /api/collection-jobs/*, /api/agent/registrations) ---
# Long random secret. Browser UI uses /sources/login (httpOnly cookie); curl uses Authorization: Bearer <token>
# SIGNALFORGE_ADMIN_TOKEN=
